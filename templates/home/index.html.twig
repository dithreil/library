{% extends 'base.html.twig' %}

{% block title %} Главная страница {% endblock %}

{% block body %}
    <div>Библиотека
    {% if authors %}
    <table class="table table-striped">
        <thead>
        <tr>
            <th>Авторы</th>
        </tr>
        </thead>
        <tbody>
        {% for a in authors %}
            <tr>
                <td>{{ a.id }}</td>
                <td>{{ a.name }}</td>
                <td>{{ a.surname }}</td>
                <td><a href="{{ path('update_author', {'id' : a.id }) }}" class="btn btn-dark">Редактировать</a></td>
                <td><a href="{{ path('show_books', {'id' : a.id }) }}" class="btn btn-dark">Список книг</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    {% else %}
        <p>Нет авторов для отображения</p>
    {% endif %}
    </div>
     {% endblock %}